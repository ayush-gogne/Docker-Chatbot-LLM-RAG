Shell | Docker Docs
Gordon
Gordon, your AI assistant for Docker docs
Search
Start a new chat
When enabled, Gordon considers the current page you're viewing
to provide more relevant answers.
Share feedback
Answers are generated based on the documentation.
Back
Manuals
Get started
Guides
Reference
Shell
Ask Gordon
Copy Markdown
View Markdown
Table of contents
sbx run shell
drops you into a Bash login shell inside a sandbox with no
pre-installed agent binary. It's useful for installing and configuring
agents manually, testing custom implementations, or inspecting a running
environment.
$
sbx run shell ~/my-project
The workspace path defaults to the current directory. To run a one-off
command instead of an interactive shell, pass it after
--
:
$
sbx run shell -- -c
"echo 'Hello from sandbox'"
Default startup command
Without extra args, the sandbox runs
bash -l
. When the first argument after
--
is a flag (begins with
-
), it's added after
-l
, so login-shell
behavior is preserved:
$
sbx run shell -- -c
"echo hi"
# runs bash -l -c "echo hi"
When the first argument is a bare word, it replaces
-l
instead.
Store credentials using
stored secrets
before running the sandbox. The proxy injects them into outbound API requests;
credentials are never stored inside the VM:
$
sbx secret
set
-g anthropic
$
sbx secret
set
-g openai
Once inside the shell, you can install agents using their standard methods,
for example
npm install -g @continuedev/cli
. For complex setups, build a
custom template
instead of installing
interactively each time.
Base image
The shell sandbox uses the
shell
base image â the common base environment
without a pre-installed agent.
Edit this page
Request changes
Table of contents